[package]
name = "bank-refinement-tutorial"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
quint-refinements = { path = "../../../bindings/rust" }

[dev-dependencies]
serde_json = "1.0"

[lints.rust]
unsafe_code = "forbid"
